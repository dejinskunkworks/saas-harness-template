/**
 * Demo tenant seed data.
 *
 * Creates a sample SaaS tenant with example data.
 * Replace with your domain-specific seed data.
 *
 * CUSTOMIZE: Update this file with realistic data for your domain.
 */

export const DEMO_TENANT = {
  name: "Acme Corp",
  region: "DEFAULT" as const,
} as const;
