# references/

Pinned third-party docs in LLM-friendly form.

Place short, self-contained references here that agents should consult when working with specific technologies. Keep each file focused on one topic and prefer plain text or lightly-formatted markdown over HTML dumps.

Suggested additions:
- `supabase-rls-llms.txt` — RLS patterns and gotchas
- `nextjs-server-actions-llms.txt` — server action conventions
- `shadcn-ui-llms.txt` — component patterns
